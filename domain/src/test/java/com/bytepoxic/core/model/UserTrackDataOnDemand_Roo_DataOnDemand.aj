// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.bytepoxic.core.model;

import com.bytepoxic.core.model.AppUserDataOnDemand;
import com.bytepoxic.core.model.TrackingType;
import com.bytepoxic.core.model.UserTrack;
import com.bytepoxic.core.model.UserTrackDataOnDemand;
import com.bytepoxic.core.service.UserService;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect UserTrackDataOnDemand_Roo_DataOnDemand {
    
    declare @type: UserTrackDataOnDemand: @Component;
    
    private Random UserTrackDataOnDemand.rnd = new SecureRandom();
    
    private List<UserTrack> UserTrackDataOnDemand.data;
    
    @Autowired
    AppUserDataOnDemand UserTrackDataOnDemand.appUserDataOnDemand;
    
    @Autowired
    UserService UserTrackDataOnDemand.userService;
    
    public UserTrack UserTrackDataOnDemand.getNewTransientUserTrack(int index) {
        UserTrack obj = new UserTrack();
        setTrackingDate(obj, index);
        setTrackingType(obj, index);
        return obj;
    }
    
    public void UserTrackDataOnDemand.setTrackingDate(UserTrack obj, int index) {
        Date trackingDate = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setTrackingDate(trackingDate);
    }
    
    public void UserTrackDataOnDemand.setTrackingType(UserTrack obj, int index) {
        TrackingType trackingType = TrackingType.class.getEnumConstants()[0];
        obj.setTrackingType(trackingType);
    }
    
    public UserTrack UserTrackDataOnDemand.getSpecificUserTrack(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        UserTrack obj = data.get(index);
        Long id = obj.getId();
        return userService.findUserTrack(id);
    }
    
    public UserTrack UserTrackDataOnDemand.getRandomUserTrack() {
        init();
        UserTrack obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return userService.findUserTrack(id);
    }
    
    public boolean UserTrackDataOnDemand.modifyUserTrack(UserTrack obj) {
        return false;
    }
    
    public void UserTrackDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = userService.findUserTrackEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'UserTrack' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<UserTrack>();
        for (int i = 0; i < 10; i++) {
            UserTrack obj = getNewTransientUserTrack(i);
            try {
                userService.saveUserTrack(obj);
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
