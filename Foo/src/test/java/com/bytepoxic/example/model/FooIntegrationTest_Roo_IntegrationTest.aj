// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.bytepoxic.example.model;

import com.bytepoxic.example.model.Foo;
import com.bytepoxic.example.model.FooDataOnDemand;
import com.bytepoxic.example.model.FooIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect FooIntegrationTest_Roo_IntegrationTest {
    
    declare @type: FooIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: FooIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: FooIntegrationTest: @Transactional;
    
    @Autowired
    FooDataOnDemand FooIntegrationTest.dod;
    
    @Test
    public void FooIntegrationTest.testCountFoos() {
        Assert.assertNotNull("Data on demand for 'Foo' failed to initialize correctly", dod.getRandomFoo());
        long count = Foo.countFoos();
        Assert.assertTrue("Counter for 'Foo' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void FooIntegrationTest.testFindFoo() {
        Foo obj = dod.getRandomFoo();
        Assert.assertNotNull("Data on demand for 'Foo' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Foo' failed to provide an identifier", id);
        obj = Foo.findFoo(id);
        Assert.assertNotNull("Find method for 'Foo' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Foo' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void FooIntegrationTest.testFindAllFoos() {
        Assert.assertNotNull("Data on demand for 'Foo' failed to initialize correctly", dod.getRandomFoo());
        long count = Foo.countFoos();
        Assert.assertTrue("Too expensive to perform a find all test for 'Foo', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Foo> result = Foo.findAllFoos();
        Assert.assertNotNull("Find all method for 'Foo' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Foo' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void FooIntegrationTest.testFindFooEntries() {
        Assert.assertNotNull("Data on demand for 'Foo' failed to initialize correctly", dod.getRandomFoo());
        long count = Foo.countFoos();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Foo> result = Foo.findFooEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Foo' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Foo' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void FooIntegrationTest.testFlush() {
        Foo obj = dod.getRandomFoo();
        Assert.assertNotNull("Data on demand for 'Foo' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Foo' failed to provide an identifier", id);
        obj = Foo.findFoo(id);
        Assert.assertNotNull("Find method for 'Foo' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyFoo(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'Foo' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void FooIntegrationTest.testMergeUpdate() {
        Foo obj = dod.getRandomFoo();
        Assert.assertNotNull("Data on demand for 'Foo' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Foo' failed to provide an identifier", id);
        obj = Foo.findFoo(id);
        boolean modified =  dod.modifyFoo(obj);
        Integer currentVersion = obj.getVersion();
        Foo merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'Foo' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void FooIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Foo' failed to initialize correctly", dod.getRandomFoo());
        Foo obj = dod.getNewTransientFoo(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Foo' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Foo' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Foo' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void FooIntegrationTest.testRemove() {
        Foo obj = dod.getRandomFoo();
        Assert.assertNotNull("Data on demand for 'Foo' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Foo' failed to provide an identifier", id);
        obj = Foo.findFoo(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Foo' with identifier '" + id + "'", Foo.findFoo(id));
    }
    
}
